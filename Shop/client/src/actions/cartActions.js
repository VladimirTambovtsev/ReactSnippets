import axios from 'axios'
import { ADD_TO_CART } from './types'

export const addProduct = _id => dispatch => {
	// axios.post('/api/users/')
	//     .then(res => dispatch({ type: ADD_PRODUCT, payload: res.data }))
}
